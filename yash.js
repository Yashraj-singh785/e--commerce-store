const createStoreBtn = document.getElementById('createStore');

createStoreBtn.addEventListener('click', () => {
    // Handle the "Create Your Store" button click 
    // (e.g., navigate to a new page, display a message, etc.)

    alert('Redirecting to store creation page!'); 
});

const addToCartBtn = document.getElementById('addToCart');

addToCartBtn.addEventListener('click', () => {
    // Handle the "Add to Cart" button click 
    // (e.g., display a success message, 
    // update a cart counter, etc.)

    alert('Product added to cart!'); 
});
